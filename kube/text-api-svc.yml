apiVersion: v1
type: Service
metadata:
    name: workspaceai-text-extractor-svc
    namespace: workspaceai-prod
spec:
    selector:
        app: workspaceai-text-extractor
    ports:
     - protocol: TCP
       port: 8000
       targetPort: 8000
    type: ClusterIP