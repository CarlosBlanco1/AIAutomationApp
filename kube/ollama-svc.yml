apiVersion: v1
kind: Service
metadata:
    name: workspaceai-ollama-svc
    namespace: workspaceai-prod
spec:
    selector:
        app: workspaceai-ollama
    ports:
     - protocol: TCP
       port: 11434
       targetPort: 11434
    type: ClusterIP